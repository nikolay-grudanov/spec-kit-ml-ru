# Research: Адаптация Spec-Kit для ML Проектов

**Branch**: `001-ml-adaptation` | **Date**: 2026-02-13 | **Spec**: [spec.md](./spec.md)

## Executive Summary

Исследование выполнено для определения подходов к адаптации GitHub Spec-Kit для ML/DS проектов. Основные фокусы: интеграция с Qwen CLI, структура ML-ориентированных шаблонов, русскоязычная локализация и внедрение ML best practices.

---

## Исследование 1: Интеграция с Qwen CLI

### Вопрос: Как интегрировать ML шаблоны с Qwen CLI для генерации артефактов?

### Решение

Использовать существующий механизм интеграции Specify CLI с AI агентами. Qwen CLI поддерживает TOML формат командных файлов с использованием плейсхолдера `{{args}}` для передачи аргументов.

### Детали реализации

**Структура директорий** (согласно AGENTS.md):
```
.qwen/
└── commands/
    ├── speckit.specify.md
    ├── speckit.plan.md
    ├── speckit.tasks.md
    └── speckit.clarify.md
```

**Формат командных файлов** (TOML):
```toml
description = "Генерация спецификации ML проекта"

prompt = """
Используй шаблон `.specify/templates/ml-spec-template.md` для генерации спецификации ML проекта на основе следующего описания: {{args}}

Требования:
- Все артефакты на русском языке
- Технические термины дублировать на английском в скобках при первом упоминании
- Включить секции: бизнес-цель, данные, метрики, ограничения
"""
```

**Аргументы команд**:
- `/speckit.specify <описание проекта>` - генерирует ML спецификацию
- `/speckit.plan` - генерирует ML план на основе существующей спецификации
- `/speckit.tasks` - генерирует список задач на основе плана
- `/speckit.clarify` - уточняет требования к проекту

### Альтернативы рассмотрены

1. **Markdown формат** (используется Claude, Cursor, opencode)
   - ❌ Не подходит для Qwen CLI
   - ❌ Требует изменения парсера

2. **Кастомный формат**
   - ❌ Сложность поддержки
   - ❌ Не соответствует стандартам Qwen CLI

3. **TOML формат** (выбран)
   - ✅ Стандарт для Qwen CLI
   - ✅ Хорошая поддержка вложенности
   - ✅ Легко парсится

### Вывод

Использовать TOML формат для командных файлов Qwen CLI с плейсхолдером `{{args}}`. Шаблоны хранить в `.specify/templates/`, командные файлы в `.qwen/commands/`.

---

## Исследование 2: Структура ML-ориентированных шаблонов

### Вопрос: Какой должна быть структура ML-ориентированных шаблонов для покрытия требований ML проектов?

### Решение

Монолитные шаблоны (как определено в FR-012) с базовым набором секций и возможностью расширения опциональными секциями (FR-011).

### Детали реализации

**Базовые секции (MVP)**:
1. **ML Spec Template** (`.specify/templates/ml-spec-template.md`):
   ```markdown
   # Спецификация ML проекта: [Название]

   ## Бизнес-цель
   - Описание бизнес-проблемы
   - Ожидаемая ценность
   - Success criteria

   ## Задача машинного обучения
   - Тип задачи (классификация, регрессия, кластеризация и т.д.)
   - Входные данные (Input)
   - Выходные данные (Output)
   - Тип обучения (supervised, unsupervised, reinforcement)

   ## Данные
   - Источник данных
   - Размер и структура
   - Качество данных
   - Требования к предобработке

   ## Метрики
   - Бизнес-метрики
   - ML метрики
   - Success thresholds

   ## Ограничения
   - Latency требования
   - Hardware ограничения
   - Производительность модели
   ```

2. **ML Plan Template** (`.specify/templates/ml-plan-template.md`):
   ```markdown
   # План реализации ML проекта: [Название]

   ## Архитектура решения
   - Data pipeline
   - Model architecture
   - Training strategy
   - Serving strategy

   ## Этапы реализации
   - Этап 1: EDA (Exploratory Data Analysis)
   - Этап 2: Baseline модель
   - Этап 3: Feature engineering
   - Этап 4: Основная модель
   - Этап 5: Evaluation и error analysis

   ## Технологический стек
   - Data processing
   - ML фреймворки
   - Experiment tracking
   - Deployment
   ```

3. **ML Tasks Template** (`.specify/templates/ml-tasks-template.md`):
   ```markdown
   # Задачи ML проекта: [Название]

   ## Подготовка данных
   - Загрузка данных
   - Data quality checks
   - Разделение на train/val/test

   ## EDA и feature engineering
   - Анализ распределений
   - Feature selection
   - Feature creation

   ## Моделирование
   - Baseline модель
   - Основная модель
   - Hyperparameter tuning

   ## Оценка и документация
   - Метрики и графики
   - Error analysis
   - Model card
   ```

4. **Data Spec Template** (`.specify/templates/data-spec-template.md`):
   ```markdown
   # Спецификация данных: [Название проекта]

   ## Обзор данных
   - Источник данных
   - Размер датасета
   - Формат данных

   ## Схема данных
   - Типы признаков
   - Описание полей
   - Взаимосвязи

   ## Качество данных
   - Missing values
   - Дубликаты
   - Outliers
   - Типы данных

   ## Стратегия предобработки
   - Обработка missing values
   - Нормализация/стандартизация
   - Encoding категориальных признаков
   - Feature engineering
   ```

**Опциональные секции (Post-MVP)**:
- MLOps секции (monitoring, alerting)
- Serving секции (API, batch inference)
- Security секции (data privacy, access controls)
- Compliance секции (regulatory requirements)

### Альтернативы рассмотрены

1. **Модульная структура шаблонов**
   - ❌ Не соответствует FR-012
   - ❌ Сложнее поддерживать
   - ❌ Требует сборки документов из нескольких файлов

2. **Монолитные шаблоны без расширяемости**
   - ❌ Не соответствует FR-011
   - ❌ Ограничивает гибкость для сложных проектов

3. **Монолитные шаблоны с расширяемостью** (выбран)
   - ✅ Соответствует FR-012 и FR-011
   - ✅ Простота использования и поддержки
   - ✅ Гибкость для разных уровней сложности

### Вывод

Использовать монолитные шаблоны с базовым набором секций и возможностью добавления опциональных секций для расширения функциональности. Базовый набор для MVP: ML Spec, ML Plan, ML Tasks, Data Spec.

---

## Исследование 3: Русскоязычная локализация

### Вопрос: Как обеспечить корректную русскоязычную локализацию всех артефактов?

### Решение

Использовать смешанный подход: русский текст для контента, английские термины в скобках при первом упоминании. Это соответствует FR-002 и constitution.

### Детали реализации

**Правила локализации** (согласно constitution раздел 11):

✅ **На русском**:
- Спецификации (.md файлы)
- Документация (README, guides, tutorials)
- Комментарии в коде
- Docstrings
- Логи и error messages
- Commit messages
- PR descriptions
- Issues и discussions
- Model cards
- Reports и presentations

🔤 **На английском**:
- Код (переменные, функции, классы)
- Названия файлов и директорий
- Git branch names
- Dependencies (requirements.txt)
- Config keys (YAML)
- API endpoints

**Пример смешанного подхода**:
```markdown
## Метрики (Metrics)

Основные бизнес-метрики (business metrics):
- Точность прогноза (prediction accuracy)
- Снижение ошибок (error reduction)

ML метрики (ML metrics):
- Accuracy: 95%
- F1-score: 0.93
- MAE: 0.05

## Гиперпараметры (Hyperparameters)

Основные гиперпараметры модели:
- max_depth: 6
- n_estimators: 100
- learning_rate: 0.1
```

**Пример docstring** (согласно constitution):
```python
def train_model(X_train: np.ndarray, y_train: np.ndarray, config: dict) -> BaseEstimator:
    """
    Обучает ML модель на тренировочных данных.
    
    Функция выполняет следующие шаги:
    1. Разделение на признаки и целевую переменную
    2. Train/test split с фиксированным random_seed
    3. Обучение модели
    
    Args:
        X_train: Признаки для обучения, shape (n_samples, n_features)
        y_train: Целевая переменная, shape (n_samples,)
        config: Конфигурация модели с гиперпараметрами
    
    Returns:
        Обученная модель
    
    Raises:
        ValueError: Если размеры X_train и y_train не совпадают
    """
    pass
```

### Альтернативы рассмотрены

1. **Полностью на русском**
   - ❌ Технические термины теряют международный контекст
   - ❌ Сложно интегрироваться с международными инструментами

2. **Полностью на английском**
   - ❌ Не соответствует требованиям пользователя
   - ❌ Снижает доступность для русскоязычных разработчиков

3. **Смешанный подход** (выбран)
   - ✅ Соответствует FR-002
   - ✅ Лучшее соотношение доступности и интернационализации
   - ✅ Соответствует constitution

### Вывод

Использовать смешанный подход: русский текст для контента и документации, английские термины для кода и конфигураций. Технические термины дублировать на английском в скобках при первом упоминании.

---

## Исследование 4: Внедрение ML Best Practices в шаблоны

### Вопрос: Как встроить ML best practices (из constitution) в структуру шаблонов?

### Решение

Включить в шаблоны секции, которые побуждают пользователей следовать best practices из constitution. Не навязывать строгий workflow, а предоставлять структуру.

### Детали реализации

**Внедрение best practices по секциям**:

1. **Reproducibility (Constitution раздел 1)**:
   ```markdown
   ## Воспроизводимость (Reproducibility)

   - Random seed: [указать значение, по умолчанию 42]
   - Experiment tracking: [MLflow / Weights&Biases / другое]
   - Версионирование данных: [DVC / другое]
   - Требования к зависимостям: [requirements.txt с точными версиями]
   ```

2. **Data Quality (Constitution раздел 2)**:
   ```markdown
   ## Качество данных (Data Quality)

   - Валидация схемы данных: [pydantic schemas / другое]
   - Data leakage prevention: [стратегия разделения данных]
   - Data quality checks: [pytest-based тесты]
   - Документирование источников данных
   ```

3. **ML Workflow (Constitution раздел 4)**:
   ```markdown
   ## Последовательность разработки (ML Workflow)

   1. Simple Baseline: [описать baseline подход]
   2. EDA: [список анализов для выполнения]
   3. Feature Engineering: [план feature engineering]
   4. Main Model Development: [основная модель]
   5. Evaluation & Error Analysis: [план оценки]

   Train/Val/Test Split:
   - Train: [70% / другое]
   - Validation: [15% / другое]
   - Test: [15% / другое]
   - Random seed: [42]
   - Stratification: [для классификации]
   ```

4. **Metrics & Experiment Tracking (Constitution раздел 5)**:
   ```markdown
   ## Метрики (Metrics)

   **Бизнес-метрики (Business metrics)**:
   - [Описание бизнес-метрики]

   **ML метрики (ML metrics)**:
   - Primary metric: [accuracy / F1 / MAE / другое]
   - Secondary metrics: [список дополнительных метрик]

   **Логирование в experiment tracking**:
   - Training loss (каждую эпоху)
   - Validation loss (каждую эпоху)
   - Primary metric
   - Secondary metrics
   - Hyperparameters
   - Model artifacts
   ```

5. **Documentation (Constitution раздел 6)**:
   ```markdown
   ## Документация (Documentation)

   Требования к документации:
   - README.md с обязательными разделами
   - Model card для каждой production модели
   - Docstrings на русском (Google style)
   - SPEC.md для проекта
   ```

6. **Deployment & MLOps (Constitution раздел 7)**:
   ```markdown
   ## Deployment (Post-MVP)

   Model Versioning:
   - Semantic versioning: v1.0.0
   - Git tags для каждой версии

   Model Registry:
   - [MLflow / другое]

   API Serving:
   - [FastAPI / другое]

   CI/CD Pipeline:
   - [описать pipeline]
   ```

7. **Ethics & Compliance (Constitution раздел 8)**:
   ```markdown
   ## Этика и Compliance (Ethics & Compliance)

   - Bias detection: [план проверки на bias]
   - Data privacy: [GDPR / другие требования]
   - Explainability: [SHAP / LIME / другое]
   - Model limitations: [ограничения модели]
   ```

### Альтернативы рассмотрены

1. **Строгое навязывание workflow**
   - ❌ Может быть слишком жестким для разных типов проектов
   - ❌ Снижает гибкость

2. **Отсутствие ML best practices**
   - ❌ Не соответствует constitution
   - ❌ Потенциально низкое качество сгенерированных артефактов

3. **Структурированное побуждение** (выбран)
   - ✅ Соответствует constitution
   - ✅ Сохраняет гибкость
   - ✅ Обеспечивает базовое качество

### Вывод

Включить в шаблоны секции для всех ключевых ML best practices из constitution. Использовать структурированный подход с указанием требований и примеров, но без строгого навязывания workflow.

---

## Исследование 5: Производительность CLI инструмента

### Вопрос: Каковы требования к производительности CLI инструмента для генерации артефактов?

### Решение

Для MVP явные требования к производительности отсутствуют (как указано в NFR-001). Производительность будет определяться реализацией и может быть оптимизирована в последующих итерациях на основе отзывов пользователей.

### Детали реализации

**Базовые ожидания** (best practices для CLI инструментов):
- Генерация спецификации: < 30 секунд для типичного проекта
- Генерация плана: < 30 секунд для типичного проекта
- Генерация задач: < 30 секунд для типичного проекта

**Факторы, влияющие на производительность**:
1. **Сложность проекта**: Чем больше деталей в проекте, тем дольше генерация
2. **Длина шаблонов**: Большие шаблоны требуют больше времени на обработку
3. **Скорость AI агента**: Зависит от Qwen API или локальной модели
4. **Объем примеров**: Наличие контекста из примеров ускоряет генерацию

**Оптимизация для Post-MVP**:
- Кеширование часто используемых шаблонов
- Параллельная генерация независимых секций
- Инкрементальная генерация (с возможностью доопределения)

### Альтернативы рассмотрены

1. **Строгие требования к производительности с самого начала**
   - ❌ Может замедлить разработку MVP
   - ❌ Не требуется для MVP (NFR-001)

2. **Оптимизация как приоритет**
   - ❌ Отвлекает от функциональных требований
   - ❌ Преждевременная оптимизация

3. **Оптимизация по запросу** (выбран)
   - ✅ Соответствует NFR-001
   - ✅ Позволяет сфокусироваться на функциональности
   - ✅ Оптимизация на основе реальных use cases

### Вывод

Явные требования к производительности для MVP отсутствуют (NFR-001). Оптимизация будет проводиться в Post-MVP итерациях на основе отзывов пользователей и реального использования. Базовые ожидания: генерация типичного артефакта < 30 секунд.

---

## Итоговые решения

| Исследование | Решение | Обоснование |
|-------------|---------|-------------|
| Интеграция с Qwen CLI | TOML формат командных файлов | Стандарт для Qwen CLI, хорошая поддержка |
| Структура шаблонов | Монолитные с расширяемостью | Соответствует FR-011 и FR-012 |
| Русскоязычность | Смешанный подход | Соответствует FR-002 и constitution |
| ML Best Practices | Структурированное побуждение | Соответствует constitution, сохраняет гибкость |
| Производительность | Оптимизация по запросу | Соответствует NFR-001 |

## Следующие шаги (Phase 1)

1. Создать `data-model.md` с описанием сущностей шаблонов и примеров
2. Создать директорию `/contracts/` с контрактами для команд
3. Создать `quickstart.md` с кратким руководством по использованию
4. Обновить agent context для opencode
5. Ре-оценить Constitution Check после завершения Phase 1

---

**Версия**: 1.0 | **Дата**: 2026-02-13 | **Статус**: Завершено
